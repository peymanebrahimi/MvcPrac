@model IEnumerable<Restaurant.Models.Dtos.RestaurantVm>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

@foreach (var item in @Model)
{
    <partial name="_Summary" model="item" />
}

